name: rag-check
on: [push, pull_request]

jobs:
  rag-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps + package (creates `arx` console script)
        run: |
          python -m pip install -U pip setuptools wheel
          python -m pip install -r requirements.txt
          # Install in editable mode to create arx command
          pip install -e .

      - name: Sanity check CLI
        run: |
          # Verify arx command is installed
          which arx
          arx --help
          # Also verify the module import works
          python -c "import importlib; m=importlib.import_module('cli.main'); print('cli.main ok:', hasattr(m,'main'))"

      - name: RAG index (uses your CLI)
        env:
          AR_ENABLE_RAG: "1"
          AR_EMBED_MODEL: "BAAI/bge-m3"
        run: |
          # Use the installed arx command
          arx memory index --src=. --namespaces=coder --persona=CODER_AI --reindex

      - name: RAG query
        env:
          AR_ENABLE_RAG: "1"
          AR_EMBED_MODEL: "BAAI/bge-m3"
        run: |
          # Use the installed arx command
          arx memory query --persona=CODER_AI --query "flow_runner" --k=3
